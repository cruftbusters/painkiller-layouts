cat <<EOF | ssh yagni -T
  cd /home/arctair/painkiller/gallery
  git fetch
  git reset origin/main --hard
  go build -o build/server
  systemctl daemon-reload --user
  systemctl restart painkiller.gallery --user
EOF
